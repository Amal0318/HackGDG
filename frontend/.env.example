# Backend API URL for Docker deployment
# Empty = use nginx proxy (production)
# Set to http://localhost:8000 for local dev
VITE_API_URL=

# WebSocket URL for real-time data streaming
# Empty = use Vite proxy (development)
# Set to ws://localhost:8000 for direct connection (or Docker)
VITE_WS_URL=

