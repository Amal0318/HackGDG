# Pathway — core stream processing + LLM xpack + local embedding
# Pinned to exact version to prevent backtracking
pathway[xpack-llm,xpack-llm-local]==0.29.1

# NOTE: sentence-transformers is installed in a separate Dockerfile step (before this)
# to prevent pip from re-resolving torch and downloading the 915 MB CUDA build.

# LLM — groq SDK used directly in main.py
# Pinned version to avoid dependency conflicts
groq==0.9.0

# FastAPI for REST API
fastapi==0.115.0
uvicorn==0.32.0

# NOTE: pdf2image, Pillow, unstructured, docling — stubbed out in create_stubs.py
# (pathway.xpacks.llm.parsers may check for them, but we don't use document parsing)

# Utilities
python-dotenv==1.1.0
