# Pathway — core stream processing + LLM xpack + local embedding
# xpack-llm-docs omitted: pipeline reads from Kafka, not documents (no docling/unstructured needed)
pathway[xpack-llm,xpack-llm-local]==0.29.1

# sentence-transformers required by SentenceTransformerEmbedder (xpack-llm-local)
sentence-transformers>=2.2.0

# LLM — groq SDK used directly in main.py
groq==0.9.0

# Utilities
python-dotenv>=1.1.0
